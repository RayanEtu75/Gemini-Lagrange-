mkdir -p certs capsule data
echo "# Accueil Gemini" > capsule/index.gmi
openssl req -x509 -newkey rsa:2048 -nodes -days 365 \
  -keyout certs/server.key -out certs/server.pem -subj "/CN=localhost"

pip install pyOpenSSL